<UserControl x:Class="LMS.SystemModule.Views.Readers.AddReaderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:LMS.SystemModule.Views.Readers"
      mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="StackPanelStyle" TargetType="{x:Type StackPanel}">
            <Setter Property="Margin" Value="30 40 0 0" />
        </Style>
    </UserControl.Resources>
    <GroupBox Header="新增读者信息" Margin="5 35 5 15">
        <StackPanel>
            <UniformGrid Rows="5">
                <StackPanel  Style="{StaticResource StackPanelStyle}">
                    <UniformGrid Columns="2">
                        <WrapPanel>
                            <TextBlock Text="图书编号：" />
                            <TextBox Text="{Binding BookId}" Width="120" />
                        </WrapPanel>
                        <WrapPanel>
                            <TextBlock Text="图书名称：" />
                            <TextBox Text="{Binding BookName}" Width="120" />
                        </WrapPanel>
                    </UniformGrid>
                </StackPanel>
                <StackPanel Style="{StaticResource StackPanelStyle}">
                    <UniformGrid Columns="2">
                        <WrapPanel>
                            <TextBlock Text="图书作者：" />
                            <TextBox Text="{Binding BookWriter}" Width="120" />
                        </WrapPanel>
                        <WrapPanel>
                            <TextBlock Text="图书价格：" />
                            <TextBox Text="{Binding BookPrice}" Width="120" />
                        </WrapPanel>
                    </UniformGrid>
                </StackPanel>
                <StackPanel  Style="{StaticResource StackPanelStyle}">
                    <UniformGrid Columns="2">
                        <WrapPanel>
                            <TextBlock Text="图书数量：" />
                            <TextBox Text="{Binding BookCount}" Width="120" />
                        </WrapPanel>
                        <WrapPanel>
                            <TextBlock Text="剩余数量：" />
                            <TextBox Text="{Binding BookSurplus}" Width="120" />
                        </WrapPanel>
                    </UniformGrid>
                </StackPanel>
                <StackPanel  Style="{StaticResource StackPanelStyle}">
                    <UniformGrid Columns="2">
                        <WrapPanel>
                            <TextBlock Text="发版时间：" />
                            <DatePicker
                                Text="{Binding PublishTime}"
                                materialDesign:HintAssist.Hint="发版时间" Width="120" />
                        </WrapPanel>
                    </UniformGrid>
                </StackPanel>
                <StackPanel Margin="10 25 10 10">
                    <Button Content="新增读者" Command="{Binding AddBookCommand}" />
                </StackPanel>
            </UniformGrid>
        </StackPanel>
    </GroupBox>
</UserControl>
